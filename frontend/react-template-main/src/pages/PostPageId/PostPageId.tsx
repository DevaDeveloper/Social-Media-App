import React, { FC } from 'react';
import Page from '../Page';
import PostAndComments from '../../components/post/PostAndComments/PostAndComments';

const PostPageId: FC = () => (
  <Page>
    <PostAndComments />
  </Page>
);

export default PostPageId;
